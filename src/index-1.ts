// Sample  TypeScript code in src/index-1.ts
export const greet = (name: string): string => {
    return `Hello, ${name}!`;
}
export const farewell = (name: string): string => {
    return `Goodbye, ${name}!`;
}
export const thankYou = (name: string): string => {
    return `Thank you, ${name}!`;
}
export const welcome = (name: string): string => {
    return `Welcome, ${name}!`;
}
export const congrats = (name: string): string => {
    return `Congratulations, ${name}!`;
}